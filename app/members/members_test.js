'use strict';

describe('app.members module', function() {

  beforeEach(module('app.members'));

  describe('members controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
